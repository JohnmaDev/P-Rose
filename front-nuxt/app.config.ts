// app.config.ts — configuración accesible en cliente y servidor
// ⚠️ NO colocar secretos aquí: todo lo de appConfig es visible en el navegador.

export default defineAppConfig({
  // Solo valores no sensibles de UI/marca
})
